------------------------------------------------------------------------------
Version 3.2.2

- Language
  * ignore_below defaults to one line
  * ignore_below reports an error if number isn't a number

------------------------------------------------------------------------------
Version 3.2.1

- Updated SnakeYaml
- Runtime regression can now catch parse errors in individual files and not abort

------------------------------------------------------------------------------
Version 3.2.0

- Language
  * Tokenizer will break up non-identifier characters into operators
  * var with typecast can optionally be allowed

------------------------------------------------------------------------------
Version 3.1.2

- Updated build system
- Updated dependencies
- Change "api" to "implementation" to avoid adding a transitive dependency that needs to be excluded

------------------------------------------------------------------------------
Version 3.1.0

- Project info is no longer retrieved using a lambda
- Removed hard coded project name
- Fixed bug when parsing benchmark results with no parameters
- regression sends an e-mail out if an exception is thrown instead of silently failing
- Benchmark names in summary are truncated

------------------------------------------------------------------------------
Version 3.0

- Updated build system
- Fixed issue where custom ignores were only applied to exact words and not substrings in a word
- Moved in several new tools for code generation, regression, and compliance

------------------------------------------------------------------------------
Version 2.1

- Added markAsAutoGenerated flag
  * If true the generated files will be marked as being auto generated using annotations
  * Only used by Java for now
  * Useful for static code tests that skip over auto generated files
- Fixed issue with generics touching class name in Java

------------------------------------------------------------------------------
Version 2.0

- 'interface' keyword is treated like 'class'
- Start of multi language support

------------------------------------------------------------------------------
Version 1.4.1

- Change to a dual license: Unlicense and MIT

------------------------------------------------------------------------------
Version 1.4

- Added //CUSTOM keyword at the start of line
  * Can be used ot specify keywords which should be ignored
  * E.g. //CUSTOM ignore FOobar64

------------------------------------------------------------------------------
Version 1.3

- All tokens that contain /**/ are ignored.
  Before ignore statements had to be explicitly added by converting then converting back
- Added //NOFILTER special token. The //NOFILTER is removed and everything after it is inserted as is
  This is to get around IntelliJ auto import removing all special encoding from an import statement

------------------------------------------------------------------------------
Version 1.2

- Attempts to debug a crash in Travis CI
  * Mostly adding flush and error messages


------------------------------------------------------------------------------
Version 1.1

- Class name is now forced to be the same as the file name
  * This change forced the parsing to become stateful

------------------------------------------------------------------------------
Version 1.0.2

- fixed a bug where a string length was hard coded